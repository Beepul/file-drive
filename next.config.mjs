/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                hostname: "impressive-peccary-425.convex.cloud"
            }
        ]
    }
};

export default nextConfig;
